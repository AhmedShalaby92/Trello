%p#notice= notice

%p
	%b Name:
	= @project.name
%p
	%b Description:
	= @project.description

= link_to 'add User Story', 'user_stories/new/'+@project.id.to_s
|
= link_to 'Edit', edit_project_path(@project)
|
\#{link_to 'Back', projects_path}

%br
%h5 Add member 
%select
	hhhh
	dhkgjb
%br
%br

%h5 That is each project User stories:
%ul
	-@project.user_stories.each do |story|
		%li
			= form_for(story, remote: true) do |f|
				= story.name
				|
				= link_to 'show','user_stories/show/'+story.id.to_s
				|
				= f.select :state, UserStory.states.keys.map{ |state| [state.titleize,state] }